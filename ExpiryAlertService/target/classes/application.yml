server:
 port: 2001
 
spring:
  datasource:
   url: jdbc:mysql://localhost:3306/citizendb
   password: 2004
   username: root

  jpa:
   show-sql: true
   database-platform: org.hibernate.dialect.MySQLDialect
   hibernate:
    ddl-auto: update
  application:
   name: EXPIRY-ALERT-SERVICE

spring:
  application:
    name: QUARANTINE-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/micropharma
    username: root
    password: root@1964
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 2071
 
 